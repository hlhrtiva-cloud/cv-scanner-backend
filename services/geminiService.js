// services/geminiService.js

// BƯỚC 2: CHƯA DÙNG.
// Bước 3 mới viết hàm thật gọi Gemini 2.5 Flash.

async function extractWithGemini(cvText, jdText, options = {}) {
  throw new Error("Gemini not implemented yet (step 3).");
}

async function analyzeWithGemini(structuredCV, jdText, options = {}) {
  throw new Error("Gemini not implemented yet (step 3).");
}

module.exports = {
  extractWithGemini,
  analyzeWithGemini
};
